"""Gate 0 experiment: audio-loudness masked-node reconstruction.

Experiment code only — kept out of core import paths. torch is imported
ONLY inside models.py / run.py, never here or in data.py, so importing the
core package stays torch-free.
"""
