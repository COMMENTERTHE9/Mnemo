"""Gate 1a: conservative continual-learning recipe vs CL baselines on
Permuted-MNIST. Separate track from gate0 — no trees involved. torch is
imported only inside methods.py / run.py (never here or in data.py).
"""
